[workspace]
channels = ["conda-forge"]
platforms = ["linux-64", "osx-arm64", "osx-64"]

[feature.pypitest.dependencies]
python = ">=3.12"

[feature.pypitest.pypi-dependencies]
humanfriendly = "*"

[environments]
pypitest = { features = ["pypitest"], solve-group = "default" }
